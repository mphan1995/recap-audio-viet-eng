# RECAP_MAX | December 22nd , 2025

AI system to recap conversation audio (VI / EN).

## Setup
```bash
pip install -r requirements.txt
sudo apt install ffmpeg
export HF_TOKEN=xxx
export OPENAI_API_KEY=xxx
